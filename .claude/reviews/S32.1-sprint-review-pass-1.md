# S32.1 sprint review, pass 1

**Reviewed**: `sprint/s32.1` against merge base `adf7993d77f4`, 53 files and
2,184 changed lines, crates: `oxml-core`, `oxml-drawing`, `oxml-layout`,
`oxml-media`, `oxml-opc`, `oxml-pdf`, `oxml-sml`, `rdocx-opc`, `rdocx-oxml`,
`rdocx-layout`, `rdocx-html`, `rdocx-pdf`, `rdocx`, `rdocx-cli`, `rdocx-wasm`,
`rpptx-oxml`, `rpptx-chart`, `rpptx-layout`, `rpptx-render`, and `rpptx`
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Milestone gate

The M6 gate is: "`cargo publish --dry-run` passes for every crate and the
`.crate` sizes are under the limit."

The locally patched `cargo publish --workspace --dry-run` packaged and verified
the exact seven stable and 12 incubating candidates without an upload. The
recursive 10 MiB assertion passed for every generated archive. The focused
`oxml-layout` inventory contained exactly 20 TTFs and four legal files, and its
archive was 3,596,626 bytes. The full gate at `8ff01f323013` also passed clippy,
the changed-crate and workspace suites, no-default features, wasm, rustdoc,
supply-chain policy, prose, generated-skill sync, and all 28 unchanged hashes.

The two cargo-release 1.1.3 trials made only preparation commits in disposable
worktrees. The stable trial moved the inherited train to 0.4.2 through root
metadata and the lockfile. The incubating trial moved all 12 explicit package
versions and workspace pins to 0.1.0. Neither trial changed README prose,
created a tag, pushed, or published.

## Not found

- Interaction: the package boundary, release preparation, publication routing,
  and CI jobs use the same exact stable and incubating family definitions.
- Duplication: no competing release helper, tracker, package gate, or workflow
  authority was introduced.
- Layering: metadata inspection found no new `oxml-*` dependency into
  `rdocx-*` or `rpptx-*`. The documented `oxml-drawing` to `rdocx-oxml` Theme
  adapter remains the sole exception.
- Harness: every feature declared no output change, every AS_BUILT entry records
  the same result, and the integrated harness matched all 28 entries.
- Gate: the package, preparation, workflow mutation, target, and full workspace
  commands were observed on the integrated result.
- Docs: the approved HLD impact files describe the current package inventory,
  release authority, tag routing, local dry-run patches, and CI matrix.
- Dependencies: no Rust dependency was added.
- Surface: the sprint changes publication eligibility and delivery automation,
  not the public Rust API.
