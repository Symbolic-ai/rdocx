# S32.2 sprint review, pass 2

**Reviewed**: `sprint/s32.2` at `db09f9634ac2eba05bf5faca12a1b75e9ed26671` against `67d22c2e770f869da47b452df7670f5c2e16961e`, 39 files, 1,199 changed lines, crates: `oxml-core`, `oxml-opc`, `oxml-media`, `oxml-layout`, `oxml-drawing`, `oxml-pdf`, `oxml-sml`, `rpptx-oxml`, `rpptx-chart`, `rpptx-layout`, `rpptx-render`, `rpptx`
**Boundary**: reviewed F-X005 recovery preparation before the registry publication prerequisite and dependent consumer cutovers
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Milestone gate

The M6 end-of-milestone gate requires `cargo publish --dry-run` to pass for
every crate and every `.crate` archive to remain below the registry size limit.
The exact locally patched 19-package workspace dry-run passed from the clean
recovery commit. No generated archive exceeded 10 MiB. Both layout archives
contained all 20 bundled TTF files and four licence or notice files, and the
`rpptx` archive contained `assets/default.pptx`.

The F-X005 recovery release boundary is ready. Every selected manifest,
workspace pin, and lockfile entry is 0.1.1. Every selected package has a
non-empty description. The workflow runs the regression that enforces those
facts before archive verification and the first upload. Real publication is
still pending the separate `/release rpptx-v0.1.1` approval and workflow. The
seven consumer-cutover stories remain pending behind that registry
prerequisite.

Evidence:

- The focused 12-package test command passed.
- `python3 -m unittest scripts/test_sprint_workflow.py` passed 22 tests.
- `cargo metadata --no-deps` selected exactly 12 publishable 0.1.1 packages
  with non-empty descriptions.
- `python3 scripts/hash_harness.py --check` passed with all 28 entries
  unchanged.
- `python3 scripts/prose_check.py` and
  `python3 scripts/sync_agent_skills.py --check` passed.
- F-X005 microscope pass 2 reported zero defects and zero smells.

## Not found

- `interaction`: the 0.1.1 recovery preserves the exact incubating allowlist
  and does not alter the stable release predicate.
- `duplication`: no duplicate production helper or release path was added.
- `layering`: no crate dependency edge changed, and no forbidden shared-crate
  direction was introduced.
- `harness`: the metadata-only recovery produced no output delta across all 28
  entries.
- `gate`: the metadata regression closes the preflight gap that allowed the
  failed 0.1.0 upload to reach crates.io.
- `docs`: architecture, backlog, build, sprint, and dependent design records
  consistently identify 0.1.1 as the complete incubating family.
- `deps`: no third-party dependency was added.
- `surface`: no Rust public API was added or changed.
