# S32.2 sprint review, pass 1

**Reviewed**: `sprint/s32.2` against `67d22c2e770f869da47b452df7670f5c2e16961e`, 36 files, 1,071 changed lines, crates: `oxml-core`, `oxml-opc`, `oxml-media`, `oxml-layout`, `oxml-drawing`, `oxml-pdf`, `oxml-sml`, `rpptx-oxml`, `rpptx-chart`, `rpptx-layout`, `rpptx-render`, `rpptx`
**Boundary**: integrated F-X005 release preparation before the registry publication prerequisite and dependent consumer cutovers
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Milestone gate

The M6 end-of-milestone gate requires `cargo publish --dry-run` to pass for
every crate and every `.crate` archive to remain below the registry size limit.
The exact locally patched 19-package workspace dry-run passed. No generated
archive exceeded 10 MiB. Both layout archives contained all 20 bundled TTF
files and four licence or notice files, and the `rpptx` archive contained
`assets/default.pptx`.

The F-X005 release boundary is ready. Real publication is intentionally still
pending the separate `/release rpptx-v0.1.0` approval and workflow. The seven
consumer-cutover stories remain pending behind that registry prerequisite, as
recorded in the sprint sequencing note. Their unfinished scope is not part of
this release-preparation review boundary.

Evidence:

- `cargo fmt --all --check` passed.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  passed.
- The changed-crate tests and the full workspace test command passed.
- Both no-default-feature layout test commands and the rdocx wasm check passed.
- Workspace documentation built with rustdoc warnings denied.
- `python3 scripts/hash_harness.py --check` passed with all 28 entries
  unchanged.
- `python3 scripts/prose_check.py` and
  `python3 scripts/sync_agent_skills.py --check` passed.
- `cargo deny check` passed its advisories, bans, licences, and sources gates.

## Not found

- `interaction`: no incompatible interaction exists within the integrated
  release-preparation delta.
- `duplication`: no duplicate production helper or implementation was added.
- `layering`: no crate dependency edge changed, and no forbidden shared-crate
  dependency direction was introduced.
- `harness`: the approved version-only change produced no output delta across
  all 28 entries.
- `gate`: the local publication and archive-size gate holds with the evidence
  above.
- `docs`: the release boundary and pending registry fact are described without
  claiming publication has already happened.
- `deps`: no third-party dependency was added.
- `surface`: no production public API was added or changed.
