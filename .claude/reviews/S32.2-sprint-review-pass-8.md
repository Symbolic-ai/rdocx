# S32.2 sprint review, pass 8

**Reviewed**: `sprint/s32.2` at `797a46cb7ea3876fd776dffb9aeebb7022db5cfd` against merge base `67d22c2e770f869da47b452df7670f5c2e16961e`, 130 files, 10,361 changed lines, crates: `oxml-core`, `oxml-drawing`, `oxml-layout`, `oxml-media`, `oxml-opc`, `oxml-pdf`, `oxml-sml`, `rdocx`, `rdocx-cli`, `rdocx-layout`, `rdocx-opc`, `rdocx-oxml`, `rdocx-pdf`, `rdocx-wasm`, `rpptx`, `rpptx-chart`, `rpptx-layout`, `rpptx-oxml`, `rpptx-render`
**Bound extension**: explicitly extended the default three-pass bound for this pass. Pass 7 found only incorrect crate-root paths in the layout migration notes. Pass 8 independently reviews the path correction at `797a46cb7ea3876fd776dffb9aeebb7022db5cfd`, confirms the pass 4 through 6 media remediations remain intact, and audits the complete sprint delta before close.
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Prior remediation

Pass 7 S1 is resolved. The migration notes now use the exact exported module
paths for `engine::layout_paragraph`, `table::layout_table`, and
`paginator::{paginate, paginate_sections}` (`CHANGELOG.md:62`). `MediaRegistry`
is correctly named without a module prefix because the crate root re-exports it
(`crates/rdocx-layout/src/lib.rs:18`).

Pass 6 B1 remains resolved. One public `MediaRegistry` keeps relationship IDs,
collision-resolved renderer IDs, bytes, and MIME types together. All four
lower-level layout and pagination entry points accept that same registry, so
external callers can preserve alternate IDs through pagination
(`crates/rdocx-layout/src/input.rs:24`).

Pass 5 S1 remains resolved. The high-level engine constructs one registry and
reuses it through body paragraphs, tables, nested tables, headers, footers,
footnotes, shape text, and pagination (`crates/rdocx-layout/src/engine.rs:55`).
Lower-level callers also construct once and pass the registry explicitly.

Pass 4 B1 remains resolved. The registry compares complete bytes and assigns
deterministic alternate IDs when compact keys collide. The forced-collision
regression verifies that inline and anchored elements retain distinct bytes,
MIME types, and final IDs (`crates/rdocx-layout/src/engine.rs:1277`). Pass 4 S1
also remains resolved, with every identified breaking layout surface documented.

## Milestone gate

The M6 gate is: "`cargo publish --dry-run` passes for every crate and the
`.crate` sizes are under the limit" (`docs/hld/14-development-backlog.md:408`).

The packaging gate holds. The F-X005 record identifies the exact patched
19-package publication dry run, archive-size checks, bundled asset inventories,
and successful publication of all 12 shared packages in GitHub Actions run
31496676517 (`docs/sprints/AS_BUILT.md:4452`). The full integrated verification
at `c9ef301` passed the workspace and 28-entry hash gates. Later remediation
does not alter manifests or package inventories.

At this HEAD, `cargo test -p rdocx-layout -p rdocx --lib --tests` passed 51
rdocx-layout tests, 62 rdocx unit tests, 72 integration tests, and 17 regression
tests. `cargo doc -p rdocx-layout --no-deps` and focused rdocx-layout clippy
passed. The hash harness matched all 28 entries, the prose checker reported zero
violations, all 25 generated skill adapters were in sync, and
`git diff --check` passed.

## Not found

- `interaction`: the eight stories and all review remediations compose without
  a cross-story correctness conflict.
- `duplication`: staged neutral implementations were removed, and each
  extracted facility has one shared owner. `MediaRegistry` is the single media
  identity and byte lookup for Word layout.
- `layering`: no new forbidden dependency from an `oxml-*` crate to an
  `rdocx-*` or `rpptx-*` crate exists. The documented
  `oxml-drawing -> rdocx-oxml` Theme adapter remains the sole exception.
- `harness`: no baseline changed. All 28 current entries match the manifest and
  their feature plans and AS_BUILT records declare unchanged output.
- `gate`: release, archive, workspace, focused remediation, rustdoc, clippy,
  prose, adapter, diff, and hash evidence was checked rather than inferred from
  story status.
- `docs`: architecture, packaging, rendering, migration, build, backlog,
  sprint, CHANGELOG, and AS_BUILT records agree with the integrated
  implementation and use valid public paths.
- `deps`: no new third-party dependency was added. Every new internal edge has
  a named consumer in the approved cutover plans.
- `surface`: every public addition or break is story-authorized and documented.
  `MediaRegistry` has high-level and direct lower-level consumers today, and no
  speculative trait, generic, feature flag, or unrelated API was introduced.
