# S32.1 sprint review, pass 3

**Reviewed**: `sprint/s32.1` at
`f098ef3b2c8a73b758b8feac87387f3b2d9ca4e1` against
`adf7993d77f4ed4a2edc9ff53e16d38e1a3ea600`, 55 files and 2,301 changed
lines, crates: `oxml-core`, `oxml-drawing`, `oxml-layout`, `oxml-media`,
`oxml-opc`, `oxml-pdf`, `oxml-sml`, `rdocx-opc`, `rdocx-oxml`,
`rdocx-layout`, `rdocx-html`, `rdocx-pdf`, `rdocx`, `rdocx-cli`,
`rdocx-wasm`, `rpptx-oxml`, `rpptx-chart`, `rpptx-layout`, `rpptx-render`, and
`rpptx`
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Milestone gate

The M6 gate is: "`cargo publish --dry-run` passes for every crate and the
`.crate` sizes are under the limit."

The gate holds. The close-time locally patched
`cargo publish --workspace --dry-run` packaged and verified the exact seven
stable and 12 incubating candidates without an upload. The recursive size
assertion found no archive above 10 MiB among all 72 generated archives. The
workflow uses that same reviewed local-source patch boundary at
`.github/workflows/publish.yml:25`, while CI rejects an oversized archive at
`.github/workflows/ci.yml:199`.

The close-time full verification also passed formatting, clippy, all 20 changed
crates, the full workspace suite, no-default features, wasm, warning-denied
rustdoc, prose, generated-skill synchronization, and supply-chain policy. The
hash harness matched all 28 entries. The consolidated feature evidence records
the exact package inventory at `docs/sprints/AS_BUILT.md:4289`, split-family
preparation at `docs/sprints/AS_BUILT.md:4319`, publication routing at
`docs/sprints/AS_BUILT.md:4353`, and the CI matrix at
`docs/sprints/AS_BUILT.md:4393`.

## Tracker closure

The S32.1 summary is arithmetically consistent. The four feature rows record
estimates of 2, 2, 2, and 1 days with one actual day each at
`docs/sprints/SPRINT_TRACKER.md:179`. These sum to 4 planned, 4 done, 0 carried,
7 estimated days, and 4 actual days at
`docs/sprints/SPRINT_TRACKER.md:48`. The velocity is therefore
`4 / 4 * 5 = 5.00`, as recorded at `docs/sprints/SPRINT_TRACKER.md:228`.

Four actual days against seven estimated days is a 42.9 percent variance, so
the escalation row is required. Its 29 pending stories and about 6 active weeks
at five stories per week agree with the backlog total and rounding at
`docs/sprints/SPRINT_TRACKER.md:265` and `docs/sprints/BACKLOG.md:33`.

## Not found

- `interaction`: package boundaries, release preparation, publication routing,
  and CI use the same exact stable and incubating family definitions.
- `duplication`: no competing release helper, tracker, package gate, or
  workflow authority was introduced.
- `layering`: no `oxml-*` crate gained a dependency into `rdocx-*` or
  `rpptx-*`. The documented `oxml-drawing` to `rdocx-oxml` Theme adapter
  remains the sole exception.
- `harness`: all four AS_BUILT entries declare no delta, and the fresh harness
  check reports 28 matches.
- `gate`: the exact local-source package graph verified successfully, every
  archive remained below the limit, and no upload occurred.
- `docs`: the approved HLD impact files describe package inventory, release
  authority, tag routing, local dry-run patches, and the CI matrix.
- `deps`: no Rust dependency was added.
- `surface`: the sprint changes publication eligibility and delivery
  automation, not the public Rust API.
- `tracker`: the sprint summary, feature rows, velocity, variance escalation,
  backlog total, and completed run state agree.
