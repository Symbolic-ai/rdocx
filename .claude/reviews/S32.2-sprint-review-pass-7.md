# S32.2 sprint review, pass 7

**Reviewed**: `sprint/s32.2` at `5a00b35d8a0110e975e5498f9e4acd3c75985c1d` against merge base `67d22c2e770f869da47b452df7670f5c2e16961e`, 129 files, 10,262 changed lines, crates: `oxml-core`, `oxml-drawing`, `oxml-layout`, `oxml-media`, `oxml-opc`, `oxml-pdf`, `oxml-sml`, `rdocx`, `rdocx-cli`, `rdocx-layout`, `rdocx-opc`, `rdocx-oxml`, `rdocx-pdf`, `rdocx-wasm`, `rpptx`, `rpptx-chart`, `rpptx-layout`, `rpptx-oxml`, `rpptx-render`
**Bound extension**: explicitly extended the default three-pass bound for this pass. Pass 6 found that external callers of the retained lower-level layout APIs could not obtain the collision-resolved media map required by pagination. Pass 7 independently reviews the public `MediaRegistry` remediation at `5a00b35d8a0110e975e5498f9e4acd3c75985c1d` and the complete sprint delta before close.
**Verdict**: 0 blocking, 1 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

### S1, migration notes name four nonexistent crate-root function paths

`CHANGELOG.md:62`
`crates/rdocx-layout/src/lib.rs:10`

The migration note says `rdocx_layout::layout_paragraph`, `layout_table`,
`paginate`, and `paginate_sections` now accept `MediaRegistry`. Those functions
are not re-exported at the crate root. Their actual public paths are
`rdocx_layout::engine::layout_paragraph`,
`rdocx_layout::table::layout_table`, and the two functions under
`rdocx_layout::paginator`. A downstream user following the documented paths
gets unresolved imports. Correct the notes to use the module-qualified paths,
or intentionally add and document root re-exports if that is the chosen public
surface.

## Nice-to-have

None.

## Prior remediation

Pass 6 B1 is resolved. `MediaRegistry` is public and re-exported from the crate
root. Its public constructor, relationship lookup, and media lookup preserve
one collision-resolved identity across both layout and pagination
(`crates/rdocx-layout/src/input.rs:24`). All four lower-level public entry points
now require that same registry, so an external caller no longer loses alternate
IDs or their bytes. The forced-collision regression still verifies distinct
inline and anchored output.

Pass 5 S1 remains resolved. `Engine::layout` constructs one registry and reuses
it through every body, table, nested table, header, footer, footnote, shape, and
pagination path (`crates/rdocx-layout/src/engine.rs:55`). Lower-level callers
also construct one registry and pass it through rather than triggering hidden
per-image reconstruction.

Pass 4 B1 remains resolved by complete-byte comparison and deterministic
alternate IDs. Pass 4 S1's three breaking surfaces remain present in the
migration notes. S1 above concerns the accuracy of four Rust paths, not an
omitted breaking surface.

## Milestone gate

The M6 gate is: "`cargo publish --dry-run` passes for every crate and the
`.crate` sizes are under the limit" (`docs/hld/14-development-backlog.md:408`).

The packaging gate holds. The F-X005 record identifies the exact patched
19-package publication dry run, archive-size checks, bundled asset inventories,
and successful publication of all 12 shared packages in GitHub Actions run
31496676517 (`docs/sprints/AS_BUILT.md:4452`). The full integrated verification
at `c9ef301` passed the workspace and 28-entry hash gates. Later remediation
does not alter manifests or package inventories.

At this HEAD, `cargo test -p rdocx-layout -p rdocx --lib --tests` passed 51
rdocx-layout tests, 62 rdocx unit tests, 72 integration tests, and 17 regression
tests. `cargo doc -p rdocx-layout --no-deps` and focused rdocx-layout clippy
passed. The hash harness matched all 28 entries, the prose checker reported zero
violations, all 25 generated skill adapters were in sync, and
`git diff --check` passed. S1 is a migration-path documentation defect outside
the formal publication and output-stability gates.

## Not found

- `interaction`: the public registry composes correctly with the high-level
  engine, recursive Word flow layout, and both pagination entry points.
- `duplication`: the sprint deletes staged neutral implementations and retains
  one shared owner for each extracted facility. `MediaRegistry` replaces the
  previously split relationship and byte maps with one implementation.
- `layering`: no new forbidden dependency from an `oxml-*` crate to an
  `rdocx-*` or `rpptx-*` crate exists. The documented
  `oxml-drawing -> rdocx-oxml` Theme adapter remains the sole exception.
- `harness`: no baseline changed. All 28 current entries match the manifest and
  their feature plans and AS_BUILT records declare unchanged output.
- `gate`: release, archive, workspace, focused remediation, rustdoc, clippy,
  prose, adapter, diff, and hash evidence was checked rather than inferred from
  story status.
- `docs`: apart from S1, the architecture, packaging, rendering, migration,
  build, backlog, sprint, CHANGELOG, and AS_BUILT records agree with the
  integrated implementation.
- `deps`: no new third-party dependency was added. Every new internal edge has
  a named consumer in the approved cutover plans.
- `surface`: the public `MediaRegistry` is required by four existing lower-level
  entry points and has both high-level and direct consumers today. No
  speculative trait, generic, feature flag, or unrelated API was introduced.
