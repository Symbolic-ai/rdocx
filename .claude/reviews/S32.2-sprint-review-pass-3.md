# S32.2 sprint review, pass 3

**Reviewed**: `sprint/s32.2` at `db3d817ae39344e3dc7b2bbce0e1e587dcb049ae` against `67d22c2e770f869da47b452df7670f5c2e16961e`, 41 files, 1,304 changed lines, crates: `oxml-core`, `oxml-opc`, `oxml-media`, `oxml-layout`, `oxml-drawing`, `oxml-pdf`, `oxml-sml`, `rpptx-oxml`, `rpptx-chart`, `rpptx-layout`, `rpptx-render`, `rpptx`
**Boundary**: reviewed F-X005 second recovery preparation before the registry publication prerequisite and dependent consumer cutovers
**Verdict**: 0 blocking, 0 should-fix, 0 nice-to-have

## Blocking

None.

## Should-fix

None.

## Nice-to-have

None.

## Milestone gate

The deferred M6 gate requires the complete shared family to exist as real
registry dependencies, released packages to pass archive verification, and all
28 hash entries to remain stable. Registry publication and the dependent
consumer cutovers are intentionally still pending the release boundary.

The 0.1.2 release candidate is ready for final SHA-bound verification. The
exact CI metadata command passed independently without invoking
`cargo-release`, while the complete local workflow suite passed 22 tests.
`cargo metadata --no-deps` selected exactly 12 publishable packages at 0.1.2
with non-empty descriptions. The exact patched 19-package workspace dry run
passed and staged every selected 0.1.2 archive. All archives were below 10 MiB,
both layout archives contained 20 TTF files and four licence or notice files,
and the `rpptx` archive contained `assets/default.pptx`. The hash harness
reported all 28 entries unchanged.

The immutable 0.1.1 workflow stopped at its metadata gate before archive
verification, uploads, or GitHub release creation. The reviewed change removes
that runner-only tool dependency without weakening the selected incubating
metadata assertions.

## Not found

- `interaction`: the 0.1.2 recovery preserves the exact incubating allowlist
  and leaves the stable predicate and package set unchanged.
- `duplication`: no duplicate production helper or release path was added.
- `layering`: no crate dependency edge changed, and no forbidden shared-crate
  direction was introduced.
- `harness`: package metadata and workflow selection produced no output delta
  across all 28 entries.
- `gate`: the targeted command exercises only the self-contained metadata
  regression required before either archive verification or upload.
- `docs`: architecture, backlog, build, sprint, and dependent plans consistently
  identify 0.1.2 as the next complete incubating family.
- `deps`: no third-party dependency was added.
- `surface`: no Rust public API was added or changed.
