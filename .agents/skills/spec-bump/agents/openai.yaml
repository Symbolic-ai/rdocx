interface:
  display_name: "Spec Bump"
  short_description: "Version the spec set. Two stages, audit then apply, so a tagg..."
  default_prompt: "Use $spec-bump to follow the repository workflow."
policy:
  allow_implicit_invocation: false
