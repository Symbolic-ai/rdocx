interface:
  display_name: "Differential Testing"
  short_description: "How to compare this workspace against an external oracle. pyt..."
  default_prompt: "Use $differential-testing to follow the repository workflow."
policy:
  allow_implicit_invocation: true
