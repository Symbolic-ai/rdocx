interface:
  display_name: "Close Sprint"
  short_description: "Close a sprint. The only command that merges to main and crea..."
  default_prompt: "Use $close-sprint to follow the repository workflow."
policy:
  allow_implicit_invocation: false
