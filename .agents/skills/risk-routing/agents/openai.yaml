interface:
  display_name: "Risk Routing"
  short_description: "Route a design plan to the mandatory reading and the extra ve..."
  default_prompt: "Use $risk-routing to follow the repository workflow."
policy:
  allow_implicit_invocation: true
