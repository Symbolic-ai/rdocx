interface:
  display_name: "Regen Fixtures"
  short_description: "Regenerate the workspace's generated artefacts. Samples, the..."
  default_prompt: "Use $regen-fixtures to follow the repository workflow."
policy:
  allow_implicit_invocation: false
