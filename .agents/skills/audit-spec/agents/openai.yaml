interface:
  display_name: "Audit Spec"
  short_description: "Scan for drift between the docs/hld/ spec set and the code th..."
  default_prompt: "Use $audit-spec to follow the repository workflow."
policy:
  allow_implicit_invocation: false
