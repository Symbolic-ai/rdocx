interface:
  display_name: "Study Crate"
  short_description: "Build a working understanding of one crate before changing it..."
  default_prompt: "Use $study-crate to follow the repository workflow."
policy:
  allow_implicit_invocation: false
