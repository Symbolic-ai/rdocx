interface:
  display_name: "Design"
  short_description: "Design a feature before implementing. Writes a design plan, n..."
  default_prompt: "Use $design to follow the repository workflow."
policy:
  allow_implicit_invocation: false
