interface:
  display_name: "Sync Status"
  short_description: "Audit consistency across the trackers, the HLD and the code...."
  default_prompt: "Use $sync-status to follow the repository workflow."
policy:
  allow_implicit_invocation: false
