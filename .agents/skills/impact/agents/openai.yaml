interface:
  display_name: "Impact"
  short_description: "Trace what a proposed change would touch, before committing t..."
  default_prompt: "Use $impact to follow the repository workflow."
policy:
  allow_implicit_invocation: false
