interface:
  display_name: "Run Sprint"
  short_description: "Run every unfinished F-ID in the current sprint. Designs firs..."
  default_prompt: "Use $run-sprint to follow the repository workflow."
policy:
  allow_implicit_invocation: false
