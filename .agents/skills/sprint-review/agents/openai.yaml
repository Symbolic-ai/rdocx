interface:
  display_name: "Sprint Review"
  short_description: "Review a whole sprint's delta before it merges. Bounded loop,..."
  default_prompt: "Use $sprint-review to follow the repository workflow."
policy:
  allow_implicit_invocation: false
