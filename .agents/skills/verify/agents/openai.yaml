interface:
  display_name: "Verify"
  short_description: "The gate. Runs formatting, lints, tests, the hash harness and..."
  default_prompt: "Use $verify to follow the repository workflow."
policy:
  allow_implicit_invocation: false
