interface:
  display_name: "Differential"
  short_description: "Run the differential corpus against python-docx and python-pp..."
  default_prompt: "Use $differential to follow the repository workflow."
policy:
  allow_implicit_invocation: false
