interface:
  display_name: "Microscope"
  short_description: "Adversarially review one feature's diff. Records findings, ch..."
  default_prompt: "Use $microscope to follow the repository workflow."
policy:
  allow_implicit_invocation: false
